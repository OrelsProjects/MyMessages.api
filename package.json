{
  "name": "mymessages-api",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "SET SLS_DEBUG=* && node --inspect node_modules\\serverless\\bin\\serverless offline --stage dev",
    "debug_mac": "SLS_DEBUG=* && node --inspect node_modules/serverless/bi /serverless offline --stage dev",
    "build-prisma": "prisma generate && mkdir -p dist/prisma && cp -r node_modules/@prisma/client/runtime dist/prisma/",
    "start": "serverless offline --stage prod --host 0.0.0.0",
    "deploy": "serverless prune -n 1 && serverless deploy --stage dev",
    "login": "serverless login"
  },
  "dependencies": {
    "@prisma/client": "^5.16.2",
    "moment": "^2.30.1",
    "qs": "^6.12.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.13.0",
    "@types/qs": "^6.9.15",
    "@types/uuid": "^9.0.8",
    "prisma": "^5.16.2",
    "serverless-plugin-typescript": "^2.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
